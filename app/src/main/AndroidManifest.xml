<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.supriya.bankingsystem">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/bank_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/bank_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BankingSystem">
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.MainActivity">

        </activity>
        <activity android:name=".activity.CustomerList" />
        <activity android:name=".activity.SendToUser" />
        <activity android:name=".activity.CustomerHistoryList" />
        <activity android:name=".activity.CustomerData" />
    </application>

</manifest>